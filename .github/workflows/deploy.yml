name: Build

on:
  push:
    branches:
      - master

jobs:
  build-and-push-container:
    name: Build and Push Thilo Container
    runs-on: ubuntu-latest
    if: github.repository == 'scout-ch/thilo'

    steps:
      - name: Check out the repo
        uses: actions/checkout@v4
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build docker container
        uses: docker/build-push-action@v5
        with:
          context: ./
          load: true
          tags: |
            ghcr.io/scout-ch/thilo/thilo_frontend:latest
      - name: Push docker container
        uses: docker/build-push-action@v5
        with:
          push: true
          context: ./
          tags: |
            ghcr.io/scout-ch/thilo/thilo_frontend:latest

  buildGHPages:
    name: Build Thilo App GHPages
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm astro build --base /thilo/
        env:
          SITE_URL: ${{ vars.SITE_URL || format('https://{0}.github.io/thilo', github.repository_owner) }}
      - uses: actions/upload-artifact@v4
        with:
          name: build-cache-gp
          path: ./build

  deployGHPages:
    name: Deploy Thilo App GHPages
    needs: buildGHPages
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/master'

    steps:
      - uses: actions/download-artifact@v4
        with:
          name: build-cache-gp
          path: ./build
      - uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build

  buildProd:
    name: Build Thilo App Prod
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/master' && github.repository == 'scout-ch/thilo'

    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm astro build
        env:
          BACKEND_URL: ${{ secrets.BACKEND_URL }}
          SITE_URL: https://thilo.scouts.ch

      - uses: actions/upload-artifact@v4
        with:
          name: build-cache-prod
          path: ./build

  deployProd:
    name: Deploy Thilo App Prod
    needs: buildProd
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/master' && github.repository == 'scout-ch/thilo'

    steps:
      - uses: actions/download-artifact@v4
        with:
          name: build-cache-prod
          path: ./build
      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USER }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftps
          server-dir: /webroot/ROOT/
          local-dir: ./build/
          exclude: |
            **/.devcontainer*
            **/.devcontainer*/**
            **/.git*
            **/.git*/**
            **/.github*
            **/.github*/**
